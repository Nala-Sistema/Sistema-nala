# Versoes fixadas: sem pin, cada redeploy instala a ultima versao de tudo e o
# app pode quebrar sozinho (foi o que aconteceu com o starlette 1.4.0).
# Estas sao as versoes que rodam no Streamlit Cloud (Python 3.14) hoje.
# Ao atualizar, confira se a versao tem wheel para cp314.
streamlit==1.54.0
# starlette 1.4.0 mudou a assinatura de GZipResponder.__init__ e quebra o
# MediaAwareGZipMiddleware do streamlit (TypeError no boot do app).
starlette<1.4
# streamlit 1.54 exige pandas<3; 2.3.3 e a unica 2.x com wheel para cp314.
pandas==2.3.3
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
openpyxl==3.1.5
bcrypt==5.0.0
google-generativeai==0.8.6
